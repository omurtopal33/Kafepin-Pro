name: KafePin Pro v3.1.26 publish
on:
  push:
    paths:
      - '.github/workflows/publish-v326-cumulative.yml'
permissions:
  contents: write
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build verify publish
        run: bash .github/v326/publish.sh
