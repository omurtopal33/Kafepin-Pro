--- a/public/kafepin-pro-yonetim.html
+++ b/public/kafepin-pro-yonetim.html
@@ -41,6 +41,15 @@
         <button id="releaseNotesClose" class="release-close">✕ Kapat</button>
       </div>
 
+      <div class="release-item" data-version="3.1.26">
+        <div class="release-title"><span>v3.1.26 — Gerçek Temiz Restart Günlüğü</span></div>
+        <ul>
+          <li><b>Restart günlüğü sunucu tarafında da sıfırlanır:</b> Yönetim Merkezi'nde Yeniden Başlat onayından sonra, Server Manager restart komutu verilmeden önce mevcut Node'un <code>liveLogs</code> belleği temizlenir.</li>
+          <li><b>Eski satırlar geri gelemez:</b> arayüz yalnız eski <code>startedAt</code> değişene kadar bekler; yeni Node farklı başlangıç kimliğiyle açıldığında yalnız yeni oturum kayıtlarını gösterir.</li>
+          <li><b>Restart güvenliği korunur:</b> <code>/restart-async</code> handoff yolu aynen kullanılır; <code>signal is aborted without reason</code> düzeltmesi korunur.</li>
+          <li><b>Diğer alanlar korunur:</b> v3.1.25 Explorer foreground düzeltmesi, v3.1.23 hızlı yedek/geri yükleme ve gün sonu düzeltmesi, v3.1.22 hız optimizasyonları aynen devam eder.</li>
+        </ul>
+      </div>
       <div class="release-item" data-version="3.1.25">
         <div class="release-title"><span>v3.1.25 — Sade Güncelleme Kartı + Temiz Restart + Explorer Stabilitesi</span></div>
         <ul>
@@ -985,6 +994,14 @@
           }catch(_){}
         }
         $('status').textContent='● Sunucu yeniden başlatılıyor...';
+
+        // v3.1.26: Eski günlük yalnız arayüzden saklanmaz; restart komutundan
+        // önce mevcut Node'un liveLogs belleği de gerçekten temizlenir. Reset
+        // tamamlanamazsa temiz restart garantisi veremeyeceğimiz için restart başlatılmaz.
+        const resetLog=await post('/admin/live-log/reset-session');
+        if(!resetLog||!resetLog.ok)throw new Error('Canlı Sistem Günlüğü yeni oturum için sıfırlanamadı');
+        oldBoot=String(resetLog.startedAt||oldBoot||liveLogBootId||'');
+        if(oldBoot)liveLogBootId=oldBoot;
         beginRestartLogWait(oldBoot);
 
         // Manager v3.1.3 bu endpointte once 202 cevabini kapatir, sonra Node'u
