--- v319/public/kafepin-pro-yonetim.html	2026-08-17 13:31:10.000000000 +0000
+++ v320/public/kafepin-pro-yonetim.html	2026-08-17 13:54:12.000000000 +0000
@@ -41,8 +41,19 @@
         <button id="releaseNotesClose" class="release-close">✕ Kapat</button>
       </div>
 
-      <div class="release-item current" data-version="3.1.19">
-        <div class="release-title"><span>v3.1.19 — Bildirimler Hızlı Git’te Kalıcı + 3.1.18 Masaüstü Geri Alma</span><span class="release-badge">GÜNCEL</span></div>
+      <div class="release-item current" data-version="3.1.20">
+        <div class="release-title"><span>v3.1.20 — Güncelleme Merkezi Bağlantı Onarımı</span><span class="release-badge">GÜNCEL</span></div>
+        <ul>
+          <li><b>Güncelleme kontrolü dayanıklı hale getirildi:</b> GitHub RAW bağlantısı Node HTTPS ile kurulamazsa Windows curl / PowerShell ağ yolu otomatik yedek olarak kullanılır.</li>
+          <li><b>Paket indirme de yedekli:</b> yeni sürüm ZIP'i Node HTTPS ile indirilemezsa Windows ağ katmanı devreye girer; mevcut SHA-256 doğrulaması aynen korunur.</li>
+          <li><b>Sürüm notları artık internete bağlı değil:</b> kurulu sürüm önce yerel /api/health üzerinden okunur. İnternet veya GitHub geçici olarak erişilemese bile “Sürüm okunuyor...” yazısında takılı kalmaz.</li>
+          <li><b>Kapsam sınırlı:</b> Bildirimler Hızlı Git'te aynı yerde kalır. EveryCafe, muhasebe, masa/çark, Telegram, gelir hesapları ve mevcut ekran düzenleri değiştirilmedi.</li>
+          <li><b>Kümülatif:</b> temiz v3.1.2 ANA STABLE üzerine doğrudan kurulabilir; v3.1.19 ve önceki gerekli düzeltmeler korunur.</li>
+        </ul>
+      </div>
+
+      <div class="release-item" data-version="3.1.19">
+        <div class="release-title"><span>v3.1.19 — Bildirimler Hızlı Git’te Kalıcı + 3.1.18 Masaüstü Geri Alma</span></div>
         <ul>
           <li><b>Tek ve kalıcı bildirim yeri:</b> 🔔 Bildirimler düğmesi <b>Hızlı Git</b> satırında <b>Bakım Modu</b> bağlantısının hemen sağına taşındı. Kafe & Çark, EveryCafe ve Kasa & Muhasebe panellerinin üçünde de görünür kalır.</li>
           <li><b>Eski yer kaldırıldı:</b> Canlı Panel içindeki <b>Son yenileme</b> bilgisinin yanında artık ikinci bir Bildirimler düğmesi yoktur.</li>
@@ -688,6 +699,20 @@
       btn.disabled=!available;
       btn.title=available?'Yeni sürümü emniyet yedeği alarak kur':'Sistem güncel'+(currentVersion?' • v'+currentVersion:'');
     }
+    async function loadLocalVersionLabels(){
+      try{
+        const r=await fetch('/api/health?_version='+Date.now(),{cache:'no-store',headers:{'Cache-Control':'no-cache'}});
+        const d=await r.json();
+        const current=String(d&&d.version||'').replace(/^v/i,'').trim();
+        if(!r.ok||!d||!d.ok||!current)return '';
+        const v='v'+current;
+        const pageLabel=$('pageVersionLabel'); if(pageLabel) pageLabel.textContent=v;
+        const cardLabel=$('releaseVersionLabel'); if(cardLabel) cardLabel.textContent=v;
+        markCurrentRelease(current,'');
+        return current;
+      }catch(_){ return ''; }
+    }
+
     async function loadUpdate(force,audit){
       try{
         const query=force?('?force=1'+(audit?'&audit=1':'')):'';
@@ -842,6 +867,7 @@
     if(updateInstallLocked) setUpdateInstallLocked(true,updateInstallTargetVersion);
     else setUpdateButton(false,'');
     async function initializeUpdatePanel(){
+      await loadLocalVersionLabels();
       await loadUpdateProgress();
       await loadUpdate(true,false);
     }
